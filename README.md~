insta-netflix-geni
==================

A superfast parallel content streaming network. 

Process to update repo:
git add . 
git commit -m "Your message here"
git push

Process to clone repo:
git clone httpgithubaddress
